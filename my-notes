# Chapter 1 我的笔记

## 由LLM驱动的智能体，显著特征为：Planning / Reasoning → Tool Use → Dynamic Adjustment/ Observing → Reasoning
   将总目标拆解为一系列执行的逻辑链，根据链条的环节调用外部工具，并根据用户反馈形成新的约束条件，规划执行新的逻辑链

## LLM 为什么让 Agent 和以前不一样? LLM Agent 一方面利用神经网络获得模式识别、语言理解与生成能力，另一方面通过计划、工具调用等结构化步骤完成复杂任务，也就是所谓的 Hybrid Agent

## 自主性： Agent 不需要认为提前规划好目标，能够自行根据目标、当前信息和环境反馈来决定下一步采取的措施

## 鲁棒性（Robustness）：输入存在噪声、变化或异常时，系统仍能保持较稳定表现的能力

## 一句话解释LLM Agent： LLM 根据目标和当前上下文（Perception）规划下一步行动（Thoughts 中的 Planning），系统将 LLM 生成的 Action 映射到外部工具执行，再把工具结果作为 Observation 返回给模型，循环迭代直到任务完成

## 一个 Tool 里放多种指标没问题，只要这些指标通常会一起被查询、参数相近、数据来源相近，而且共同回答一个业务问题。可以根据维度来分指标，维度内部不同的tool调用不同的指标，但这些指标都是同一维度的

## Workflow 是预先定义好的结构化流程，Agent 则保留更高自主性去达成目标

## 评估 LLM主要看输出,评估 Agent 要看过程加结果，除了准确性以外，还要看效率、成本、过程的合理性、是否合规或越权等



## Chapter 1 初识智能体｜核心框架

1. **Agent 的基本定义**
   智能体是能够**感知环境（Sensor）→ 自主决策 → 通过执行器（Actuator）采取行动 → 达成目标**的实体。Agent 早于 LLM 出现，LLM 只是让 Agent 的理解、规划和生成能力大幅增强。([GitHub][1])

2. **传统 Agent → LLM Agent 的核心变化**
   传统 Agent 更多依赖预设规则、模型和流程；LLM Agent 可以直接理解复杂自然语言任务，并具备：
   **Planning / Reasoning → Tool Use → Dynamic Adjustment**
   即拆解目标、调用工具、根据反馈动态调整后续计划。([GitHub][1])

3. **Agent Loop：最核心运行机制**

Observation
→ Thought / Planning
→ Tool Selection
→ Action
→ Environment State Change
→ New Observation
→ 下一轮循环

Agent 通过不断“感知—思考—行动—观察”逐步完成任务。([GitHub][1])

4. **Thought / Action / Observation**

Thought：我要做什么、为什么
Action：调用哪个工具 + 参数
Parser：解析 Action 并触发 Tool
Tool：真正执行操作
Observation：工具返回后整理出的信息


Observation 会重新加入上下文，让 LLM 决定下一步。([GitHub][1])

5. **一个基础 LLM Agent 的组成**


System Prompt
+ LLM
+ Tools
+ Tool Registry
+ Parser
+ Prompt / Context History
+ Agent Loop


其中 LLM 是“决策大脑”，Tool 提供外部能力，程序负责把这些组件连接成完整系统。

6. **Workflow vs Agent**


Workflow：
流程和判断逻辑提前设计好
→ 按固定路径执行
→ 稳定、可控

Agent：
给定目标和工具
→ LLM 自主规划部分执行路径
→ 根据实时反馈动态调整


二者不是非黑即白，实际产品中经常是 **固定 Workflow + 局部 Agent 决策**。([GitHub][1])

7. **产品视角最重要的一句话**
   设计 Agent 时核心不是“用了多少 AI”，而是：

> **为了完成用户目标，哪些决策适合交给 LLM，哪些信息需要通过 Tool 获取，以及 Agent 应该拥有多大自主权。**

